<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Simply Physics</title>
  <style>
    /* ---------- RESET ---------- */
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: "Segoe UI", Arial, sans-serif;
      background: linear-gradient(135deg, #c9d6ff, #e2e2e2);
      color: #222;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }
    h3 {
      font-family: 'Times New Roman', Times, serif;
      color: black;
    }

    /* ---------- HEADER ---------- */
    header {
      background: linear-gradient(90deg, #4f46e5, #3b82f6);
      color: white;
      text-align: center;
      font-size: 2em;
      padding: 20px;
      font-weight: bold;
      letter-spacing: 0.5px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
    }

    /* ---------- NAV BAR ---------- */
    nav {
      background-color: #1e293b;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 25px;
      padding: 10px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
      position: relative;
      z-index: 1000;
      flex-wrap: wrap;
    }

    .nav-item {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .nav-btn {
      background: none;
      border: none;
      color: white;
      font-weight: 600;
      font-size: 1rem;
      padding: 10px 16px;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
      min-width: 240px;
      text-align: center;
    }

    .nav-btn:hover,
    .nav-btn.active {
      background-color: #334155;
      transform: translateY(-2px);
    }

    /* ---------- DROPDOWN ---------- */
    .dropdown {
      position: absolute;
      top: calc(100% + 6px);
      left: 50%;
      transform: translateX(-50%);
      background: white;
      width: 240px;
      border-radius: 8px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
      overflow: hidden;
      max-height: 0;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .dropdown.show {
      max-height: 600px;
      opacity: 1;
      visibility: visible;
    }

    .dropdown a {
      display: block;
      padding: 10px 15px;
      color: #1e293b;
      text-decoration: none;
      transition: background-color 0.2s;
      font-size: 0.95rem;
    }

    .dropdown a:hover {
      background-color: #e2e8f0;
    }

    /* ---------- CONTENT ---------- */
    .content {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: transparent;
    }

    .atom-icon {
      position: relative;
      width: 200px;
      height: 200px;
      animation: float 4s ease-in-out infinite;
    }

    .atom-icon svg {
      width: 100%;
      height: 100%;
      filter: drop-shadow(12px 12px 0 rgba(0, 0, 0, 0.2));
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-12px); }
    }

    /* ---------- RESPONSIVE ---------- */
    @media (max-width: 700px) {
      header { font-size: 1.5em; }
      nav { flex-direction: column; gap: 10px; }
      .nav-btn { min-width: unset; width: 100%; }
      .dropdown {
        position: static;
        transform: none;
        box-shadow: none;
        width: 100%;
        max-height: none;
        opacity: 1;
        visibility: visible;
      }
    }
  </style>
</head>
<body>
  <header>Simply Physics</header>

  <nav>
    <!-- AP Physics 1 -->
    <div class="nav-item">
      <button class="nav-btn">AP Physics 1</button>
      <div class="dropdown">
        <h4>Individual Presentations for each topic will be coming soon!</h4>
        <a href="kinematics.html">Kinematics</a>
        <a href="force-and-translational-dynamics.html">Force and Translational Dynamics</a>
        <a href="work-energy-and-power.html">Work, Energy, and Power</a>
        <a href="linear-momentum.html">Linear Momentum</a>
        <a href="torque-and-rotational-dynamics.html">Torque and Rotational Dynamics</a>
        <a href="energy-and-momentum-of-rotating-systems.html">Energy and Momentum of Rotating Systems</a>
        <a href="oscillations.html">Oscillations</a>
        <a href="fluid-dynamics.html">Fluid Dynamics</a>
        
      </div>
    </div>

    <!-- AP Physics 2 -->
    <div class="nav-item">
      <button class="nav-btn">AP Physics 2</button>
      <div class="dropdown">
        <a href="#">Coming Soon!</a>
      </div>
    </div>

    <!-- AP Physics C Electricity and Magnetism -->
    <div class="nav-item">
      <button class="nav-btn">AP Physics C Electricity & Magnetism</button>
      <div class="dropdown">
        <a href="#">Coming Soon!</a>
      </div>
    </div>

    <!-- AP Physics C Mechanics -->
    <div class="nav-item">
      <button class="nav-btn">AP Physics C Mechanics</button>
      <div class="dropdown">
        <a href="#">Coming Soon!</a>
      </div>
    </div>
  </nav>
    
    <div class="nav-item">
      <button class="nav-btn"><h3>About the Founder</h3></button>
      <div class="dropdown">
        <a href="#">Coming Soon!</a>
      </div>
    </div>
  </nav>

  <div class="content">
    <div class="atom-icon">
      <svg viewBox="0 0 100 100">
        <circle cx="50" cy="50" r="8" fill="black" />
        <ellipse cx="50" cy="50" rx="35" ry="15" fill="none" stroke="black" stroke-width="2" />
        <ellipse cx="50" cy="50" rx="35" ry="15" fill="none" stroke="black" stroke-width="2" transform="rotate(60 50 50)" />
        <ellipse cx="50" cy="50" rx="35" ry="15" fill="none" stroke="black" stroke-width="2" transform="rotate(120 50 50)" />
      </svg>
    </div>
  </div>

  <script>
    // Toggle dropdowns on click
    document.querySelectorAll('.nav-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const dropdown = btn.nextElementSibling;
        const openDropdown = document.querySelector('.dropdown.show');

        // Close any other open dropdowns
        if (openDropdown && openDropdown !== dropdown) {
          openDropdown.classList.remove('show');
          openDropdown.previousElementSibling.classList.remove('active');
        }

        // Toggle current dropdown
        dropdown.classList.toggle('show');
        btn.classList.toggle('active');
      });
    });

    // Close dropdowns when clicking outside
    document.addEventListener('click', e => {
      if (!e.target.closest('.nav-item')) {
        document.querySelectorAll('.dropdown.show').forEach(d => {
          d.classList.remove('show');
          d.previousElementSibling.classList.remove('active');
        });
      }
    });
  </script>
</body>
</html>
